augroup ctrlp_config
    autocmd!
    let g:ctrlp_match_window='bottom,order:ttb'
    let g:ctrlp_switch_buffer=0 " Jump to tab AND buffer if already open
    let g:ctrlp_working_path_mode=0
augroup END
